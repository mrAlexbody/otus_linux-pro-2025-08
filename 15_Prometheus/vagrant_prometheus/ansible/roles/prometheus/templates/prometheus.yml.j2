global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'prometheus'
    scrape_interval: 5s
    static_configs:
      - targets:
{% for target in prometheus_targets %}
        - '{{ target }}'
{% endfor %}

